server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${{ MySQL.MYSQL_URL }}}

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false